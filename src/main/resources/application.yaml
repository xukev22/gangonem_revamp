spring:
  application:
    name: gangonem
  datasource:
    url: jdbc:h2:mem:testdb
  profiles:
    active: local

server:
  servlet:
    context-path: /
  ssl:
    key-store: ./certs/keystore.jks
    key-store-password: password
    key-store-type: jks
    key-alias: selfsigned
  port: 8443
 
---
spring:
  config:
    activate:
      on-profile: local
